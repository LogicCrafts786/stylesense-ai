[pytest]
minversion = 8.0
addopts = -ra -q --strict-markers --cov=src --cov-report=term-missing
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: unit tests
    integration: integration tests requiring external API keys
